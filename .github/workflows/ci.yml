name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  test:
    # Xcode 27, the toolchain the code is written against.
    runs-on: xcode-27
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: swift test
